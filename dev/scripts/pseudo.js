// REACT FLOW

// Before start - go through and rename things stuff that MAKES SENSE even if it's long and wierd



// Store Inputs in State
	// handlechange listener on inputs piped to state objects
	// Setstate on submit
	// Push to newItems - newItems goes to Firebase - setState from firebase array

// Passengers List
	// Create empty array in Items which lists passengers
	// when someone says NO to hasCar, find item in Item's array that has more than 0 spots, and push that item to the passengers array.
	// When this happens, decrease the number of spots by 1
	// Iterate over passengers array and print usernames to card

// 


